function sortArray(input) {
    //let myArr =
    input.sort().sort((a,b) => a.length > b.length).forEach(x => console.log(x));
 //  myArr.forEach(x => console.log(x));
}
sortArray(["gamma","alpha","beta", "gamma"]);
sortArray(["test","Deny","omen", "Default"]);
